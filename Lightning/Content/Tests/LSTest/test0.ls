// LightningScript 
// April 25, 2021
// Comments are // 

// methods are exposed by the engine and are called exactly how they are in C#
Logging.Log("Hello world!"); // semicolon optional

// Dynamically typed
a = "a"
b = 2

// Functions have C-like syntax
function Test() 
{
	Logging.Log("I'm a script!")
}

if (b == 2) // statements have C-like syntax 
{
	
}

// Infinite loop
while (true)
{
	Test();
}